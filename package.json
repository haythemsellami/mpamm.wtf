{
  "name": "mpamm.wtf",
  "private": true,
  "version": "0.1.0",
  "description": "Real-time analytics dashboard for propAMMs on Monad \u2014 execution quality vs pair-terms CEX references, volume, markouts, and quote-update gas burn. Venue-agnostic adapter registry.",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "build": "npm -w web run build",
    "typecheck": "npm -w server run typecheck && npm -w web run typecheck",
    "start": "npm -w server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/haythemsellami/mpamm.wtf.git"
  },
  "engines": {
    "node": ">=24"
  }
}
